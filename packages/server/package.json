{
  "name": "@liskat/server",
  "version": "0.0.1",
  "description": "Liskat authoritative game server: matchmaking lobby + Skat rounds over WebSocket.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "start:node": "node --experimental-strip-types src/index.ts",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": {
    "@liskat/engine": "*",
    "nodemailer": "^6.9.0",
    "ws": "^8.18.0"
  },
  "license": "AGPL-3.0-or-later"
}
