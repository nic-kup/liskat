node_modules/
dist/
build/
.DS_Store
*.log
.env
.env.*
coverage/

# local dev helpers / preview
.join-bots.mjs
.qm-bots.mjs

# feedback storage (local)
packages/server/data/

# bot evolution harness + runs (scripts are throwaway; only the evolved weights,
# baked into packages/engine/src/bot-params.ts, are committed)
packages/engine/experiments/
